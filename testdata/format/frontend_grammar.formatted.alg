algoritmo "grammar" // ação
var // declarations
  _Count, Mixed: real // action
inicio // body
  { ação }
  Mixed <- 1.0
  escreval(MIXED + 1.0 - 2) // ação
fimalgoritmo // action
ignored @ "unterminated
