algoritmo "grammar" // ação
var // declarations
  _Count, Mixed: real; // action
inicio // body
  { ação }
  Mixed <- 1e
  escreval(MIXED + 1e-2) // ação
fimalgoritmo // action
ignored @ "unterminated
