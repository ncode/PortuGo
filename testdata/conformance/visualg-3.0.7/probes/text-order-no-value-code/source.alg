algoritmo "text order"
funcao Texto(idx: inteiro): caractere
inicio
escreval(idx)
retorne "abc"
fimfuncao
funcao Numero(idx: inteiro): real
inicio
escreval(idx)
retorne idx + 0.9
fimfuncao
inicio
escreval("BEFORE",carac(abs()),Texto(9))
escreval("DONE")
fimalgoritmo
