algoritmo "comparison edges"
inicio
escreval((verdadeiro) = (falso))
escreval((verdadeiro) <> (falso))
escreval((verdadeiro) < (falso))
escreval((verdadeiro) > (falso))
escreval((verdadeiro) <= (falso))
escreval((verdadeiro) >= (falso))
fimalgoritmo
