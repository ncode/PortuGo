algoritmo "text order"
funcao Texto(idx: inteiro): caractere
inicio
escreval(idx)
retorne "abc"
fimfuncao
funcao Numero(idx: inteiro): real
inicio
escreval(idx)
retorne idx + 0.9
fimfuncao
inicio
escreval(copia(Texto(1),Numero(2),Numero(3),Texto(4)))
fimalgoritmo
