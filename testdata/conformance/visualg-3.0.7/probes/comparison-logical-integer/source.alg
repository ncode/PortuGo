algoritmo "comparison"
inicio
escreval((verdadeiro) = (1))
escreval((verdadeiro) <> (1))
escreval((verdadeiro) < (1))
escreval((verdadeiro) > (1))
escreval((verdadeiro) <= (1))
escreval((verdadeiro) >= (1))
fimalgoritmo
