algoritmo "comparison"
inicio
escreval((verdadeiro) = ("x"))
escreval((verdadeiro) <> ("x"))
escreval((verdadeiro) < ("x"))
escreval((verdadeiro) > ("x"))
escreval((verdadeiro) <= ("x"))
escreval((verdadeiro) >= ("x"))
fimalgoritmo
